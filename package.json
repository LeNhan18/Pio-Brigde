{
  "name": "pio-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Cross-chain bridge PIO -> wPIO (Pione Zero -> Goerli)",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy": "node scripts/deploy.js",
    "deploy:local": "node scripts/deploy-local.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^4.9.6",
    "dotenv": "^16.4.5",
    "ethers": "^5.7.2",
    "hardhat": "^2.22.10"
  }
}


